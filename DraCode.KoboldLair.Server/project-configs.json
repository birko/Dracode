{
  // Project resource configurations for KoboldLair
  // Controls how many kobolds can run in parallel per project
  
  // Default limit for projects not explicitly configured
  "defaultMaxParallelKobolds": 1,
  
  // Per-project configurations
  "projects": [
    {
      "projectId": "example-project",
      "projectName": "Example Project",
      "maxParallelKobolds": 2
    },
    {
      "projectId": "high-priority-project",
      "projectName": "High Priority Project",
      "maxParallelKobolds": 4
    }
  ]
}
